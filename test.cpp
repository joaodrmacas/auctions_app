#include <iostream>
#include <sstream>

int main() {
    // std::istringstream reqstream("123456 12345678\n");

    // std::string UID, req_pass;

    // if (reqstream >> UID) {
    //     std::cout << "UID: " << UID << std::endl;
    // }

    // if (reqstream >> req_pass) {
    //     std::cout << "Password: " << req_pass << std::endl;
    // }

    // if (!reqstream.eof()) {
    //     std::cout << "There is more data in the stream." << std::endl;

    //     // Read and print the remaining content
    //     std::string remaining_content;
    //     getline(reqstream, remaining_content);
    //     std::cout << "Remaining content: \"" << remaining_content << "\"" << std::endl;
    // }

    

    return 0;
}